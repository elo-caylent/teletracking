variable "zone_name" {
  type = string
}

variable "records" {
  type = list(object({
    name    = string
    type    = string
    ttl     = number
    records = list(string)
    alias   = object({
      name    = string
      zone_id = list(string)
    })
  }))
}
